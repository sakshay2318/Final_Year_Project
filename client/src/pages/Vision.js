import React from "react";
import VisionManager from "../components/VisionManager";

const Vision = () => {
    return (
        <div>
        <VisionManager />
        </div>
    );
};

export default Vision;